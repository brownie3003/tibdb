<header class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
        <div class="container">
            <%= link_to "TiB Assignment DB", root_path, id: "logo" %>
            <nav>
                <ul class="nav pull-right">
                    <li><%= link_to "Home", root_path %></li>
                    <% if signed_in? %>
                        <li class="dropdown">
                            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                Assignments <b class="caret"></b>
                            </a>
                            <ul class ="dropdown-menu" role="menu">
                                <li><%= link_to "View all", assignments_path %></li>
                                <li><%= link_to "Add Assignment", new_assignment_path %></li>
                            </ul>
                        </li>
                    <% else %>
                        <li><%= link_to "Assignments", assignments_path %></li>
                    <% end %>
                    <% if signed_in? %>
                        <li class="dropdown">
                            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                Account <b class="caret"></b>
                            </a>
                            <ul class ="dropdown-menu" role="menu">
                                <li><%= link_to "Users", users_path %></li>
                                <% if manager? %>
                                <li><%= link_to "Add Department", new_department_path %></li>
                                <% end %>
                                <li><%= link_to "Profile", current_user %></li>s
                                <li class="divider"></li>
                                <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
                            </ul>
                        </li>
                    <% else %>
                        <li><%= link_to "Sign in", signin_path %></li>
                    <% end %>
                </ul>
            </nav>
        </div>
    </div>
</header>